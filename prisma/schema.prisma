generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model wishes {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  createdAt String
  message   String
  name      String
}
